Default locale is english, but you can also use norwegian (no )or swedish (sv).
Example on how to norwegian titles in the report:
```
<configuration>
        .. .
	<locale>no</locale>
</configuration>
```